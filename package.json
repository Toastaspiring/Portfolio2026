{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Louis — Dev Portfolio. Static SPA with per-language pre-rendered OG previews.",
  "scripts": {
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
